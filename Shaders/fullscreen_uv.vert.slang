// Segitiga penutup layar yang juga menghasilkan uv. Dipakai pass yang mencuplik
// di antara texel — bloom — dan karena itu butuh koordinat pecahan, bukan hanya
// posisi pikselnya.

struct Varyings {
    float4 position : SV_Position;
    float2 uv : TEXCOORD0;
};

[shader("vertex")]
Varyings main(uint vertexId : SV_VertexID) {
    Varyings output;
    output.uv = float2((vertexId << 1) & 2, vertexId & 2);
    output.position = float4(output.uv * 2.0 - 1.0, 0.0, 1.0);
    return output;
}
